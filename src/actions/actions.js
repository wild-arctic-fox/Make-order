export const addProduct = (el) => {
    return {
        type: "ADD_PRODUCT",
        value: el
    }
};

export const addCustomer = (el) => {
    return {
        type: "ADD_CUSTOMER",
        value: el
    }
};

export const addOrder = (el) => {
    return {
        type: "ADD_ORDER",
        value: el
    }
};